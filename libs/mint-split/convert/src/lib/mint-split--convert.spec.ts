import { mintSplitConvert } from './mint-split--convert';

describe('mintSplitConvert', () => {
    it('should work', () => {
        expect(mintSplitConvert()).toEqual('mint-split--convert');
    });
});
